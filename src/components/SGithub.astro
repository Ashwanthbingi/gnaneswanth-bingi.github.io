<section id="github" class="s-info s-info--secondary" data-intersect>
  <div class="s__inner">
    <h2 class="s__title">GitHub</h2>

    <p class="s__text">
      I build and share most of my backend, AI, and distributed-systems
      projects in the open. Take a look at my repositories and feel free to
      star, fork, or contribute.
    </p>

    <a
      href="https://github.com/Ashwanthbingi"
      target="_blank"
      rel="noopener"
      class="s__btn"
    >
      Visit My GitHub
    </a>
  </div><!-- .s__inner -->
</section>

<style lang="scss">
  .s-info {
    @include flex(column, center, center);

    position: relative;

    padding: 6rem 2rem;
    text-align: center;

    @include mq(phone) {
      padding: 3.5rem 1.5rem;
    }

    .s__inner {
      @include flex(column, center, center);

      max-width: 40rem;
      gap: 1.5rem;
    }

    .s__title {
      font: 400 4rem / 1 font-family(editorial);
      text-transform: uppercase;

      @include mq(phone) {
        font-size: 2.5rem;
      }
    }

    .s__text {
      font: 400 16px / 1.6 font-family(fraktion);
      opacity: 0.8;
    }

    .s__btn {
      display: inline-flex;
      align-items: center;

      margin-top: 0.5rem;
      padding: 0.9em 2em;

      border-radius: 999rem;

      font: 700 14px / 1 font-family(fraktion);
      letter-spacing: 0.05em;
      text-decoration: none;
      text-transform: uppercase;

      transition: opacity 0.3s ease(in-out-expo);

      &:hover {
        opacity: 0.8;
      }
    }

    &--secondary {
      background: color(secondary);
      color: color(primary);

      .s__btn {
        background: color(primary);
        color: color(secondary);
      }
    }
  }
</style>
