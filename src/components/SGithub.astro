<section id="github" class="s-info s-info--secondary" data-intersect>
  <div class="s__inner" data-reveal-group>
    <h2 class="s__title" data-reveal="mask">GitHub</h2>

    <p class="s__text" data-reveal>
      I build and share most of my backend, AI, and distributed-systems
      projects in the open. Take a look at my repositories and feel free to
      star, fork, or contribute.
    </p>

    <div class="s__btn-wrap" data-reveal>
      <a
        href="https://github.com/Ashwanthbingi"
        target="_blank"
        rel="noopener"
        class="c-btn c-btn--invert"
      >
        <span class="c-btn__label">Visit My GitHub</span>
      </a>
    </div><!-- .s__btn-wrap -->
  </div><!-- .s__inner -->
</section>

<style lang="scss">
  .s-info {
    @include flex(column, center, center);

    position: relative;

    padding: 6rem 2rem;
    text-align: center;

    @include mq(phone) {
      padding: 3.5rem 1.5rem;
    }

    .s__inner {
      @include flex(column, center, center);

      max-width: 40rem;
      gap: 1.5rem;
    }

    .s__title {
      font: 400 4rem / 1 font-family(editorial);
      text-transform: uppercase;

      @include mq(phone) {
        font-size: 2.5rem;
      }
    }

    .s__text {
      font: 400 16px / 1.6 font-family(fraktion);
      opacity: 0.8;
    }

    .s__btn-wrap {
      /*
       * The reveal sits on this wrapper, not the button: the shared .c-btn uses
       * `scale` for its press state and the reveal system animates `scale` too.
       */
      margin-top: 0.5rem;
    }

    &--secondary {
      background: color(secondary);
      color: color(primary);
    }
  }
</style>
